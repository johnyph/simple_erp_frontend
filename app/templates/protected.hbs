<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'protected.dashboard' class='navbar-brand'}}SimpleERP{{/link-to}}
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-user"></i> {{session.current_user.full_name}} <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <li><a href="#" {{ action 'invalidateSession' }}><i class="fa fa-sign-out"></i>Logout</a></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="row">
  <div class="col-sm-3 col-md-2 sidebar">
    <ul class="nav nav-sidebar">
      {{#if session.current_user.isAdmin}}
      <li>
        {{#link-to 'organizations'}}
          <i class="fa fa-university"></i>
          Διαχείριση Εταιριών
        {{/link-to}}
      </li>
      {{/if}}
    </ul>
  </div>

  <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
    {{outlet}}
  </div>
</div>

{{ember-notify}}
