<div class="row">
  <div class="col-sm-12">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <h3 class="panel-title"><i class="fa fa-university"></i> Εταιρία - {{model.name}}</h3>
      </div>
      <div class="panel-body">
        <div class="btn-group" role="group" aria-label="...">
          {{#link-to 'organization.edit' model.id  class="btn btn-primary"}}
            <i class="fa fa-university"></i> Στοιχεία Εταιρίας
          {{/link-to}}
          {{#link-to 'users' model.id  class="btn btn-primary"}}
            <i class="fa fa-users"></i> Χρήστες
          {{/link-to}}
        </div>
        <div class="top-20"></div>
        <div class="row">
          <div class="col-sm-12">
            <div class="panel panel-default">
              <div class="panel-body">
                {{outlet}}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>